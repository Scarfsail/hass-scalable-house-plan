# Copy this file to `.env` and fill in the values for your local Home Assistant.
# `.env` is git-ignored and must never be committed.

# Base URL of the locally running Home Assistant instance.
HASS_URL=http://localhost:8123/

# Credentials of a Home Assistant admin user used to log in for the tests.
HASS_USERNAME=
HASS_PASSWORD=
